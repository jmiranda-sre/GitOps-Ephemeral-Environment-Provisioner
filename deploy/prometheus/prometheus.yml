global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'ephemeral-env-provisioner'
    static_configs:
      - targets: ['app:9090']
    metrics_path: /metrics
    scrape_interval: 10s

rule_files:
  - 'alerts.yml'
